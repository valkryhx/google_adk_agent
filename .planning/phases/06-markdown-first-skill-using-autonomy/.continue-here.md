---
phase: 06-markdown-first-skill-using-autonomy
task: 0
total_tasks: 0
status: paused
last_updated: 2026-04-14T08:34:43.006Z
---

<current_state>
Phase 6 planning artifacts now exist, but this should still be treated as exploratory planning context rather than a fully formalized, officially transitioned phase. The current official `.planning/STATE.md` and roadmap narrative still describe Phase 5 as the completed formal phase awaiting closure/verification. So the practical reality is: current thinking has moved into Phase 6, while official planning state has not yet been fully reconciled.

The key strategic correction from this session is that Kairos should not become a strict JSON-first rules executor. The correct direction is markdown-first, LLM-core, skill-using long-running autonomy: Kairos maintains persistent markdown work docs, uses LLM intelligence for understanding/planning/execution content/verification/replan, and uses rules only as safety/materialization/audit boundaries.

A transitional LLM planner/verifier/action-payload implementation was added during exploration, but live testing showed strict `response_format={"type":"json_object"}` is unstable with the current model gateway and frequently times out. The later user clarification explicitly changed the target away from over-constrained JSON and toward clear markdown outputs plus autonomous use of existing skills.
</current_state>

<completed_work>

- Clarified Kairos identity: `main_web_start_steering.py` is the interactive Agent host; Kairos must be the long-running autonomous work runtime.
- Clarified Kairos necessity: not “can call skills” but “can maintain long-term work objects, wake/resume, read/update markdown work docs, orchestrate skills across turns, and self-correct.”
- Audited reusable skills: `file_editor`, `codebase_search`, `dex`, `search_exp`, `playwright/playwright-cli`, `agent_team`, and `compactor` are directly relevant to Kairos autonomy.
- Decided rules should remain, but only as guardrails/materializers/idempotency/audit/host bridge; they must no longer be the semantic planner.
- Created exploratory Phase 6 plan: `.planning/phases/06-markdown-first-skill-using-autonomy/06-IMPLEMENTATION-PLAN.md`.
- Implemented transitional scaffolding: `llm_planner.py`, `llm_verifier.py`, action payload models, runtime/API visibility, and dispatcher experiments.
- Ran multiple regression suites during exploration; focused Kairos suites were green at several points (e.g. 118/122/123 passed), but final live behavior still needs renewed verification after architecture correction.
</completed_work>

<remaining_work>

- Formalize Phase 6 in official planning docs (`STATE.md` / `ROADMAP.md`) or explicitly keep current work as exploratory planning.
- Convert planner outputs from strict JSON-first to markdown-first artifacts with only minimal tolerant extraction where needed.
- Add a real Kairos skill bridge so runtime can safely call existing `skill_load` and skills/tools under allowlist/audit.
- Promote markdown docs to first-class working memory for requirement/design/codegen/verification.
- Make verification/replan results drive actual next actions and markdown patches.
- Re-run live Flask+SQLite+HTML task after markdown-first + skill bridge changes to prove sustained autonomous progression.
- Update ROADMAP/PROJECT/STATE if Phase 6 becomes official roadmap scope rather than exploratory plan.
</remaining_work>

<decisions_made>

- Kairos must be LLM-core, not rule-core.
- Markdown is the preferred primary artifact for Kairos intelligence; strict JSON must not dominate the design.
- Existing skills are not optional; Kairos should become an autonomous long-running orchestrator over the host’s skill ecosystem.
- Rules remain necessary, but only as safety boundaries, idempotency, materialization, host bridge, and audit trail.
- Phase 6 should be a formal phase: `06-markdown-first-skill-using-autonomy`, not just a Phase 5 patch.
</decisions_made>

<blockers>
- Strict structured JSON calls time out against the current model gateway; use markdown-first and tolerant parsing instead.
- No formal Kairos-to-skill bridge exists yet; current skill loading belongs to `SteeringSession` and main Agent.
- `private_key.yaml` is tracked and locally modified; never stage it.
- `requirements/` contains live session artifacts; review before committing or exclude.
</blockers>

<context>
The current best architecture is: `SteeringSession` = interactive host, `KairosRuntime` = long-running autonomy coordinator, markdown work docs = externalized work memory, skills/tools = execution capability layer. Kairos should not duplicate the main Agent; it should schedule, remember, resume, and coordinate skills across time around persistent work docs.

Be careful: the repo now contains exploratory transitional code that may look like the final architecture but is not fully aligned with the latest user correction. The next session should not continue making JSON schemas stricter. It should stabilize Phase 6 around markdown-first skill-using autonomy.
</context>

<next_action>
Start with: read `.planning/phases/06-markdown-first-skill-using-autonomy/06-IMPLEMENTATION-PLAN.md`, then audit the current uncommitted code for JSON-first transitional pieces. Decide whether to refactor `llm_planner.py` and `runtime.py` toward markdown-first outputs and a real skill bridge before running more live tests.
</next_action>
