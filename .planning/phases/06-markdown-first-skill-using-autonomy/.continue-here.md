---
phase: 06-markdown-first-skill-using-autonomy
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-04-18T23:16:00+08:00
---

<current_state>
Phase 6 已进入执行中段，第一波“LLM-only 决策 + 轻量人工介入”链路已落地并通过回归。当前重点已从“是否能继续”转为“从 Kairos 专属入口注册并长期自治推进”。
</current_state>

<completed_work>
- 落地 attention 人工介入闭环：
  - 新增 `POST /api/sessions/{session_id}/kairos/attention/respond`
  - 前端 Kairos 面板新增 Need Your Input 展示与回复入口
  - 用户回复直接写入 `work.md` 的 `Replan Notes`，供 LLM 下一轮读取
- 落地 LLM-only follow-up 决策主链：
  - runtime 增加 `llm_only_decision_enabled`
  - 启用后 follow-up 只接受 LLM 结果；无 LLM 时阻塞而非规则回退
- continuation 行为优化：
  - 某个文档任务被 ask_user 阻塞时，可继续推进同会话内其他可行动任务（“只暂停该任务”）
- 相关测试已通过（阶段回归集）：
  - `tests/kairos/test_models.py`
  - `tests/kairos/test_document_protocol.py`
  - `tests/kairos/test_continuation.py`
  - `tests/kairos/test_api.py`
  - `tests/kairos/test_frontend_script_kairos_ui.py`
  - `tests/kairos/test_runtime.py`
</completed_work>

<remaining_work>
- 新增 Kairos 专属任务注册入口（不经 `/api/chat`）：
  - `POST /api/sessions/{session_id}/kairos/work/register`
  - 前端 Kairos 面板支持直接注册 requirement/work
- 打通“专属入口 -> markdown 工件 -> LLM 规划 -> skill/执行 -> verification/replan”闭环的 live 路径
- 在保持 ask_user 轻量的前提下，强化“LLM 主导推进”的默认行为和可观测性
</remaining_work>

<decisions_made>
- Phase 6 必须坚持 LLM 作为唯一驱动核心，规则层仅承担安全边界与状态迁移。
- Kairos 不接管用户普通 `/chat` 流；任务注册必须来自 Kairos 面板/专用 API。
- `ask_user` 只做轻量兜底，不构建复杂人工工作流；用户回复沉淀到 markdown，继续由 LLM 读取推进。
</decisions_made>

<blockers>
- `private_key.yaml` 仍为 tracked 且本地 modified；任何提交必须显式排除。
- 工作区存在大量非本 phase 变更，需精确 `git add`。
</blockers>

<next_action>
先落地 Kairos 专属 `work/register` 接口与面板接线，再补齐 API/runtime/frontend 回归，最后用专属入口做一次端到端自治验证（观察 markdown 产物与 LLM 推进证据）。
</next_action>
