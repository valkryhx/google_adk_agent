---
phase: 01-autonomous-continuation-mvp
plan: 03
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - src/adk_agent/main_web_start_steering.py
  - src/adk_agent/kairos/api.py
  - src/adk_agent/static/script.js
  - tests/dex/test_tools.py
  - tests/kairos/test_live_http_kairos_demo_outputs_regression.py
  - tests/kairos/live_http_kairos_demo_outputs_regression.py
  - tests/kairos/test_frontend_script_kairos_ui.py
autonomous: true
requirements:
  - KAI-01
  - KAI-02
  - KAI-03
  - VER-02
  - VER-03
user_setup: []
must_haves:
  truths:
    - SteeringSession can safely create report follow-up Dex tasks and register them back into Kairos handoff.
    - Live HTTP demo no longer manually registers the report task.
    - Frontend/API can expose workflow, planned actions, and blocked reason for phase-1 debugging.
  artifacts:
    - src/adk_agent/main_web_start_steering.py
    - src/adk_agent/kairos/api.py
    - src/adk_agent/static/script.js
    - tests/kairos/live_http_kairos_demo_outputs_regression.py
    - tests/kairos/test_live_http_kairos_demo_outputs_regression.py
  key_links:
    - runtime continuation decision flows through a host callback into real Dex task creation.
    - live-http regression verifies report.json is produced without manual report registration.
---

<objective>
接通宿主层 follow-up 执行入口，完成真实 Dex + live HTTP 自动 report 闭环，并把 phase-1 自治状态暴露给 API/前端。

Purpose: 把 runtime 内部 continuation decision 变成真实可见、可验证、可演示的 autonomous continuation 行为。
Output: host callback + live HTTP regression upgrade + workflow/planned_actions UI/API visibility。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

  <context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-autonomous-continuation-mvp/01-CONTEXT.md
@.planning/phases/01-autonomous-continuation-mvp/01-RESEARCH.md
@src/adk_agent/main_web_start_steering.py
@src/adk_agent/kairos/api.py
@src/adk_agent/static/script.js
@skills/dex/tools.py
@tests/kairos/live_http_kairos_demo_outputs_regression.py
@tests/kairos/test_live_http_kairos_demo_outputs_regression.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 提供宿主层受控 follow-up 执行入口</name>
  <files>src/adk_agent/main_web_start_steering.py, tests/dex/test_tools.py</files>
  <read_first>src/adk_agent/main_web_start_steering.py, skills/dex/tools.py, src/adk_agent/kairos/runtime.py, .planning/phases/01-autonomous-continuation-mvp/01-CONTEXT.md, .planning/phases/01-autonomous-continuation-mvp/01-RESEARCH.md</read_first>
  <action>在 `SteeringSession` 上增加受控 callback，用于：1) 以当前 `user_id` 创建 Dex task；2) 启动或注册 follow-up handoff；3) 记录 kairos_event / activity log；4) 保持 state-only persistence。禁止让 runtime 直接 new `DexManager()` 自己执行。必须确保 follow-up task 不会落到 global namespace。同步在 tests/dex/test_tools.py 或相关集成测试中增加对 user namespace 与 follow-up summary 的断言。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/dex/test_tools.py -q</verify>
  <acceptance_criteria>
    - src/adk_agent/main_web_start_steering.py contains a host-level callback for Kairos follow-up Dex task creation
    - callback passes current user_id when creating DexManager or invoking Dex task creation
    - tests/dex/test_tools.py contains a real-Dex follow-up integration case
    - `pytest tests/dex/test_tools.py -q` exits 0
  </acceptance_criteria>
  <done>runtime 已有安全宿主执行入口，能够把 continuation decision 转成真实 Dex follow-up task。</done>
</task>

<task type="auto">
  <name>Task 2: 扩展 API 与前端显示 phase-1 自治状态</name>
  <files>src/adk_agent/kairos/api.py, src/adk_agent/static/script.js, tests/kairos/test_frontend_script_kairos_ui.py</files>
  <read_first>src/adk_agent/kairos/api.py, src/adk_agent/static/script.js, tests/kairos/test_frontend_script_kairos_ui.py, src/adk_agent/kairos/runtime.py</read_first>
  <action>在 API/status 输出中明确暴露 active_workflow、planned_actions、blocked_reason，并在 `src/adk_agent/static/script.js` 中新增相应 formatter/render 逻辑。Phase 1 的前端面板就要能显示 workflow、planned actions、blocked reason，而不是只看 tracked tasks。同步补前端字符串测试，确保 helper 与新 DOM 区块存在。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_frontend_script_kairos_ui.py tests/kairos/test_api.py -q</verify>
  <acceptance_criteria>
    - src/adk_agent/kairos/api.py status output includes `active_workflow`
    - src/adk_agent/kairos/api.py status output includes `planned_actions`
    - src/adk_agent/kairos/api.py status output includes `blocked_reason`
    - src/adk_agent/static/script.js contains workflow formatter/helper
    - src/adk_agent/static/script.js contains planned actions formatter/helper
    - tests/kairos/test_frontend_script_kairos_ui.py verifies workflow/planned actions/blocked reason visibility
  </acceptance_criteria>
  <done>Phase 1 自治状态对用户与开发者已可见，便于调试与 live 演示。</done>
</task>

<task type="auto">
  <name>Task 3: 升级 live HTTP 回归为自动 report 闭环</name>
  <files>tests/kairos/live_http_kairos_demo_outputs_regression.py, tests/kairos/test_live_http_kairos_demo_outputs_regression.py</files>
  <read_first>tests/kairos/live_http_kairos_demo_outputs_regression.py, tests/kairos/test_live_http_kairos_demo_outputs_regression.py, tests/kairos/test_runtime.py, docs/实现phase-2的kairos/2026-04-04-kairos-live-demo-design.md</read_first>
  <action>把 live HTTP regression 中手工注册 report task 的步骤删掉，改为：phase-1 tasks 仍由脚本创建并注册；随后等待 KAIROS 自动创建并推进 report。断言必须明确：1) 出现 auto-created report 相关 recent event；2) `demo_outputs/report.json` 仍然生成；3) 最终 `tracked_dex_task_ids == []`；4) 最终 `mode == idle`；5) 不需要脚本手工 register report task。同步保留 pytest wrapper 测试入口。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_live_http_kairos_demo_outputs_regression.py -q</verify>
  <acceptance_criteria>
    - tests/kairos/live_http_kairos_demo_outputs_regression.py no longer manually registers a report task
    - tests/kairos/live_http_kairos_demo_outputs_regression.py asserts report artifact exists after Kairos auto progression
    - tests/kairos/live_http_kairos_demo_outputs_regression.py asserts final mode is `idle`
    - tests/kairos/live_http_kairos_demo_outputs_regression.py asserts tracked_dex_task_ids are empty at convergence
    - `pytest tests/kairos/test_live_http_kairos_demo_outputs_regression.py -q` exits 0
  </acceptance_criteria>
  <done>Phase 1 最关键的 live 证据链成立：report task 由 KAIROS 自动推进，而非人工注册。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/dex/test_tools.py -q`
- [ ] `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_frontend_script_kairos_ui.py tests/kairos/test_api.py -q`
- [ ] `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_live_http_kairos_demo_outputs_regression.py -q`
</verification>

<success_criteria>

- 宿主回调安全创建 follow-up Dex task
- API/前端完整展示 phase-1 自治状态
- live HTTP regression 证明 report task 不再依赖人工手工注册
- Phase 1 autonomous continuation MVP 在真实服务链路中闭环
</success_criteria>

<output>
After completion, create `.planning/phases/01-autonomous-continuation-mvp/01-03-SUMMARY.md`
</output>
