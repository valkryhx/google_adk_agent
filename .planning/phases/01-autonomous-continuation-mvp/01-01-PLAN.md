---
phase: 01-autonomous-continuation-mvp
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adk_agent/kairos/models.py
  - src/adk_agent/kairos/__init__.py
  - tests/kairos/test_models.py
autonomous: true
requirements:
  - KAI-02
  - KAI-04
user_setup: []
must_haves:
  truths:
    - Kairos state can express active workflow, planned actions, blocked reason, and policy.
    - Legacy kairos state still loads without breaking phase-2 behavior.
  artifacts:
    - src/adk_agent/kairos/models.py
    - tests/kairos/test_models.py
  key_links:
    - load_kairos_state()/dump_kairos_state() round-trip preserves new phase-1 autonomy fields.
---

<objective>
扩展 KAIROS 最小状态模型，为 autonomous continuation MVP 提供稳定的数据契约。

Purpose: 让后续 continuation engine、runtime、API、前端都围绕统一状态工作。
Output: 可序列化/反序列化的 phase-1 workflow-aware state。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-autonomous-continuation-mvp/01-CONTEXT.md
@.planning/phases/01-autonomous-continuation-mvp/01-RESEARCH.md
@src/adk_agent/kairos/models.py
@src/adk_agent/kairos/__init__.py
@tests/kairos/test_models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 锁定 phase-1 workflow-aware state 契约</name>
  <files>tests/kairos/test_models.py</files>
  <read_first>src/adk_agent/kairos/models.py, tests/kairos/test_models.py, .planning/phases/01-autonomous-continuation-mvp/01-CONTEXT.md</read_first>
  <action>在 tests/kairos/test_models.py 先补失败测试，明确 phase-1 最小状态模型必须包含 active_workflow、stages、planned_actions、blocked_reason、policy。测试必须覆盖：1) 新状态 round-trip；2) legacy state 兼容；3) policy 默认值稳定；4) 不要求 continuation_history/retries。使用具体断言，不要写“supports phase-3 fields”这种模糊描述。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_models.py -q</verify>
  <acceptance_criteria>
    - tests/kairos/test_models.py contains `test_state_round_trip_preserves_workflow_and_planned_actions`
    - tests/kairos/test_models.py contains `test_load_legacy_state_fills_phase3_defaults`
    - tests/kairos/test_models.py contains `test_policy_defaults_are_stable`
  </acceptance_criteria>
  <done>模型测试先失败，明确暴露缺失字段与序列化逻辑。</done>
</task>

<task type="auto">
  <name>Task 2: 在 models.py 实现最小 workflow-aware state</name>
  <files>src/adk_agent/kairos/models.py, src/adk_agent/kairos/__init__.py</files>
  <read_first>src/adk_agent/kairos/models.py, src/adk_agent/kairos/__init__.py, .planning/phases/01-autonomous-continuation-mvp/01-CONTEXT.md, .planning/phases/01-autonomous-continuation-mvp/01-RESEARCH.md</read_first>
  <action>在 src/adk_agent/kairos/models.py 中新增最小必要结构：KairosWorkflowStage、KairosWorkflow、KairosPlannedAction、KairosContinuationPolicy，并在 KairosState 中加入 active_workflow、planned_actions、blocked_reason、policy。保持现有 TriggerKind / KairosState 风格，继续使用 dataclass + load_kairos_state()/dump_kairos_state()。不要加入 continuation_history、retry orchestration、supervisor metadata 等超出 phase-1 的字段。同步更新 src/adk_agent/kairos/__init__.py 导出新增类型。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_models.py -q</verify>
  <acceptance_criteria>
    - src/adk_agent/kairos/models.py contains `class KairosWorkflowStage`
    - src/adk_agent/kairos/models.py contains `class KairosWorkflow`
    - src/adk_agent/kairos/models.py contains `class KairosPlannedAction`
    - src/adk_agent/kairos/models.py contains `class KairosContinuationPolicy`
    - src/adk_agent/kairos/models.py contains `active_workflow`
    - src/adk_agent/kairos/models.py contains `planned_actions`
    - src/adk_agent/kairos/models.py contains `blocked_reason`
    - src/adk_agent/kairos/models.py contains `policy`
    - src/adk_agent/kairos/__init__.py exports the new Kairos types
  </acceptance_criteria>
  <done>新模型可 round-trip，旧状态兼容，且不破坏现有 phase-2 状态加载。</done>
</task>

<task type="auto">
  <name>Task 3: 回归验证最小状态模型未破坏 phase-2 baseline</name>
  <files>tests/kairos/test_models.py</files>
  <read_first>tests/kairos/test_models.py, tests/kairos/test_runtime.py, src/adk_agent/kairos/models.py</read_first>
  <action>运行与状态模型直接相关的回归，确认最小模型扩展没有破坏 phase-2 baseline。若需要，补一个断言确保当前 tracked_dex_task_ids/schedules/recent_events 的默认与 dump 行为仍保持有效。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_models.py tests/kairos/test_runtime.py -q</verify>
  <acceptance_criteria>
    - `pytest tests/kairos/test_models.py tests/kairos/test_runtime.py -q` exits 0
    - No existing phase-2 model/runtime tests are deleted or weakened
  </acceptance_criteria>
  <done>状态模型扩展被证明确实能作为后续 continuation/runtime 的稳定底座。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_models.py -q`
- [ ] `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_models.py tests/kairos/test_runtime.py -q`
- [ ] `src/adk_agent/kairos/models.py` 中新字段能与 legacy state 共存
</verification>

<success_criteria>

- 所有模型任务完成
- 最小 workflow-aware state 已可序列化
- 旧 phase-2 行为未被破坏
- 后续 continuation/runtime 可以直接依赖这些字段
</success_criteria>

<output>
After completion, create `.planning/phases/01-autonomous-continuation-mvp/01-01-SUMMARY.md`
</output>
