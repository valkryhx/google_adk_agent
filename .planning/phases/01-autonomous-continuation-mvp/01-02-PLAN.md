---
phase: 01-autonomous-continuation-mvp
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adk_agent/kairos/continuation.py
  - src/adk_agent/kairos/workflows.py
  - src/adk_agent/kairos/runtime.py
  - tests/kairos/test_continuation.py
  - tests/kairos/test_runtime.py
autonomous: true
requirements:
  - KAI-01
  - KAI-03
  - KAI-04
  - VER-01
user_setup: []
must_haves:
  truths:
    - Runtime can evaluate completed phase-1 inputs and derive a continuation decision.
    - KAIROS can enqueue an internal continuation trigger instead of waiting for a manual wake.
    - Runtime does not create duplicate report follow-up actions.
  artifacts:
    - src/adk_agent/kairos/continuation.py
    - src/adk_agent/kairos/workflows.py
    - src/adk_agent/kairos/runtime.py
    - tests/kairos/test_continuation.py
    - tests/kairos/test_runtime.py
  key_links:
    - runtime._poll_dex() invokes continuation evaluation.
    - workflow template describes the `sales/traffic/quality -> report` convergence rule.
---

<objective>
新增 continuation engine 与 workflow template，并接入 runtime 主循环，形成 autonomous continuation 的核心判定链。

Purpose: 让 KAIROS 在已跟踪任务完成后，不只是记录 completed，而是能产生下一步 continuation decision。
Output: rule-based continuation engine + runtime integration + runtime-level tests。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-autonomous-continuation-mvp/01-CONTEXT.md
@.planning/phases/01-autonomous-continuation-mvp/01-RESEARCH.md
@src/adk_agent/kairos/runtime.py
@src/adk_agent/kairos/models.py
@tests/kairos/test_runtime.py
@tests/dex/test_tools.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: 用测试先锁定 continuation 规则与 workflow template</name>
  <files>tests/kairos/test_continuation.py, tests/kairos/test_runtime.py</files>
  <read_first>tests/kairos/test_runtime.py, .planning/ROADMAP.md, .planning/phases/01-autonomous-continuation-mvp/01-CONTEXT.md, .planning/phases/01-autonomous-continuation-mvp/01-RESEARCH.md</read_first>
  <action>新增 tests/kairos/test_continuation.py，并在 tests/kairos/test_runtime.py 中补 phase-1 autonomous continuation 的失败测试。测试至少覆盖：1) `sales/traffic/quality` 全 completed 且 artifact 条件满足时，返回 create_report continuation decision；2) artifact 缺失时返回 blocked/waiting_input；3) 同一 workflow follow-up 不重复；4) runtime 在 `_poll_dex()` 后会生成 internal continuation trigger 或 planned action，而不是只写 completed brief。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_continuation.py tests/kairos/test_runtime.py -q</verify>
  <acceptance_criteria>
    - tests/kairos/test_continuation.py contains `test_all_inputs_ready_returns_create_report_decision`
    - tests/kairos/test_continuation.py contains `test_missing_artifact_returns_blocked_decision`
    - tests/kairos/test_continuation.py contains `test_duplicate_follow_up_is_suppressed_by_fingerprint`
    - tests/kairos/test_runtime.py contains `test_completed_inputs_enqueue_internal_continuation_trigger`
  </acceptance_criteria>
  <done>测试明确暴露 phase-2 runtime 当前还不会自动续推的缺口。</done>
</task>

<task type="auto">
  <name>Task 2: 实现 continuation engine 与 workflow template</name>
  <files>src/adk_agent/kairos/continuation.py, src/adk_agent/kairos/workflows.py</files>
  <read_first>src/adk_agent/kairos/models.py, src/adk_agent/kairos/runtime.py, .planning/phases/01-autonomous-continuation-mvp/01-CONTEXT.md, .planning/phases/01-autonomous-continuation-mvp/01-RESEARCH.md, docs/实现phase-2的kairos/2026-04-04-kairos-live-demo-design.md</read_first>
  <action>新增 `src/adk_agent/kairos/continuation.py` 与 `src/adk_agent/kairos/workflows.py`。第一版 continuation engine 必须保持 rule-based，不引入额外自治库。workflow template 只支持当前 `sales/traffic/quality -> report` 的 demo_report_pipeline。decision 结构必须能表达：create_dex_task、blocked、emit_brief。不要加入 supervisor、webhook、multi-template registry 等超出 phase-1 的系统。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_continuation.py -q</verify>
  <acceptance_criteria>
    - src/adk_agent/kairos/continuation.py contains `class ContinuationEngine`
    - src/adk_agent/kairos/workflows.py contains `demo_report_pipeline`
    - continuation engine returns a create-report style decision when all inputs are ready
    - continuation engine returns blocked/waiting_input style decision when required artifact is missing
  </acceptance_criteria>
  <done>continuation 规则独立成模块，runtime 后续只需调用而不内嵌全部 policy 细节。</done>
</task>

<task type="auto">
  <name>Task 3: 接 continuation 到 runtime 主循环</name>
  <files>src/adk_agent/kairos/runtime.py, tests/kairos/test_runtime.py</files>
  <read_first>src/adk_agent/kairos/runtime.py, src/adk_agent/kairos/models.py, src/adk_agent/kairos/continuation.py, src/adk_agent/kairos/workflows.py, tests/kairos/test_runtime.py</read_first>
  <action>在 `src/adk_agent/kairos/runtime.py` 中把 continuation evaluation 接到 `_poll_dex()` 后。具体目标状态：当 phase-1 输入任务全部 completed 且 workflow 条件满足时，runtime 生成 internal continuation trigger 或 planned action，而不是停在 `tracked_dex_task_ids == []` 后直接回到纯 idle。必须实现 follow-up 去重，避免同一 workflow 连续 tick 重复创建 report 动作。`get_status()` 需要暴露 active_workflow、planned_actions、blocked_reason 的最小状态输出。</action>
  <verify>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_continuation.py tests/kairos/test_runtime.py -q</verify>
  <acceptance_criteria>
    - src/adk_agent/kairos/runtime.py contains continuation engine wiring
    - src/adk_agent/kairos/runtime.py contains `TriggerKind.INTERNAL` execution path that is actually reachable
    - src/adk_agent/kairos/runtime.py get_status includes `active_workflow`
    - src/adk_agent/kairos/runtime.py get_status includes `planned_actions`
    - src/adk_agent/kairos/runtime.py get_status includes `blocked_reason`
    - `pytest tests/kairos/test_continuation.py tests/kairos/test_runtime.py -q` exits 0
  </acceptance_criteria>
  <done>runtime 已经具备 autonomous continuation 的核心决策链，但还未真正创建 Dex follow-up task。</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_continuation.py tests/kairos/test_runtime.py -q`
- [ ] `src/adk_agent/kairos/runtime.py` 中 `_poll_dex()` 后存在 continuation evaluation
- [ ] `get_status()` 暴露 workflow / planned_actions / blocked_reason
</verification>

<success_criteria>

- continuation engine 已存在且保持 rule-based
- runtime 能在输入任务收敛后生成下一步动作
- duplicate follow-up 被抑制
- Phase 1 的 decision 语义有稳定 runtime 测试保护
</success_criteria>

<output>
After completion, create `.planning/phases/01-autonomous-continuation-mvp/01-02-SUMMARY.md`
</output>
