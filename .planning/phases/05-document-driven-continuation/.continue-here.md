---
phase: 05-document-driven-continuation
task: 0
total_tasks: 0
status: resumed_for_gap_fixing
last_updated: 2026-04-13T08:47:07Z
---

<current_state>
Phase 5 code and verification remain green for the originally shipped document-first visibility loop, but live testing against a new non-hardcoded software requirement showed the real gap: document-backed work becomes planning-visible and persists as a work doc, yet it does not materialize into an executable next step. The next active work is to implement the minimal generic execution bridge, not to optimize a Flask-specific example.
</current_state>

<completed_work>

- Verified Phase 5 historical code path still passes the existing todo/demo live regressions
- Ran a real `/api/chat` requirement-intake experiment with a new software project requirement and confirmed two facts:
  - non-English requirement text currently misses the supported-requirement shortcut because `_looks_like_supported_requirement()` only checks narrow English keywords
  - English requirement text successfully creates `requirements/<session>/work.md`, `document_work_items`, and `pending_requirements`, but no executable follow-up or step progression occurs
- Performed systematic debugging and confirmed the root cause in code:
  - document-backed `continue_workflow` currently materializes to `continue_workflow_scan`
  - document-backed `create_follow_up` is hard-blocked (`follow-up not available for document work yet`)
  - `apply_decisions()` only creates internal triggers for `create_dex_task`
- Updated `05-RESEARCH.md` with a generic progression-contract recommendation:
  - `Document Work Item -> WorkProgressSnapshot -> GateEvaluation -> ExecutableAction`
  - rule-based materializer + minimal `StepAttempt` persistence + explicit verification gates
- Refined the first implementation wave to five target files:
  - `src/adk_agent/kairos/continuation.py`
  - `src/adk_agent/kairos/orchestration.py` (new)
  - `src/adk_agent/main_web_start_steering.py`
  - `src/adk_agent/kairos/models.py`
  - `tests/kairos/test_continuation.py`

</completed_work>

<remaining_work>

- Implement the minimal generic execution bridge for document-backed work
- Replace document-backed `continue_workflow_scan` with executable materialization (`run_dex_task` / `ask_user` / `record_blocked` / `sleep_until_signal`)
- Add the first version of `WorkProgressSnapshot`, `GateEvaluation`, `ExecutableAction`, and `StepAttempt`
- Extend runtime/host bridge behavior enough that a new document-backed requirement can autonomously produce at least one executable next step
- After the first wave, expand runtime/API/dex/live HTTP regressions to prove real progression rather than planning-only visibility

</remaining_work>

<decisions_made>

- Do not optimize around the Flask example; it is only a validation probe for generic autonomy
- Keep the design document-first and additive; do not regress to runtime-only memory or hardcoded workflow-template sprawl
- Preserve the existing 4B planning surfaces (`last_planning_result`, history/API visibility, pending triggers) and add the missing execution bridge layer beneath them
- Keep execution authorization rule-based and auditable; let LLMs help with document interpretation and prose, not free-form execution authority

</decisions_made>

<blockers>
- `private_key.yaml` is still tracked and locally modified; never stage it
- The first implementation wave is still unstarted; current repo behavior remains planning-visible but execution-dead for non-hardcoded document-backed work
- `_looks_like_supported_requirement()` is too narrow for multilingual user input, but this is secondary to the main execution-bridge gap

</blockers>

<context>
The key mental model to preserve: the problem is no longer whether Kairos can write a requirement work doc. It can. The real problem is the missing bridge from document-backed planning winner to runtime-executable next step. The first code wave should therefore focus on generic orchestration primitives and trigger materialization, not on richer requirement prose or project-specific templates.
</context>

<next_action>
Start with the first implementation wave on the five target files. Begin by writing/expanding continuation tests so document-backed winners must materialize executable actions, then add the minimal orchestration contract and wire it through continuation, host follow-up creation, and state persistence.
</next_action>
