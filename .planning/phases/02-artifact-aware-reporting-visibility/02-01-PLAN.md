---
phase: 02-artifact-aware-reporting-visibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/adk_agent/kairos/models.py
  - src/adk_agent/kairos/dex_bridge.py
  - src/adk_agent/kairos/continuation.py
  - src/adk_agent/kairos/runtime.py
  - src/adk_agent/kairos/api.py
  - tests/kairos/test_runtime.py
  - tests/kairos/test_api.py
  - tests/dex/test_tools.py
autonomous: true
requirements: [RPT-01, RPT-02]
user_setup: []
must_haves:
  truths:
    - "用户能从 KAIROS status/API 看到最近完成或失败任务的结构化结果摘要，而不是只有 completed/failed。"
    - "用户能看到 KAIROS 为什么继续、为什么停住，以及当前还缺什么。"
    - "KAIROS 进入 blocked 或 waiting_input 时，缺失条件以完整条件树暴露，而不是只有一句 blocked reason。"
  artifacts:
    - path: "src/adk_agent/kairos/models.py"
      provides: "共享 reporting / explanation / condition-tree 数据结构与状态序列化"
      contains: "KairosState"
    - path: "src/adk_agent/kairos/runtime.py"
      provides: "统一生成 summary / explanation / condition_tree 的 status payload"
      contains: "get_status"
    - path: "src/adk_agent/kairos/api.py"
      provides: "兼容增强的 /kairos/status 输出"
      contains: "register_kairos_routes"
  key_links:
    - from: "src/adk_agent/kairos/dex_bridge.py"
      to: "src/adk_agent/kairos/runtime.py"
      via: "Dex snapshot -> reporting summary payload"
      pattern: "result_summary|error_summary|log_path|artifacts"
    - from: "src/adk_agent/kairos/continuation.py"
      to: "src/adk_agent/kairos/runtime.py"
      via: "continuation decision -> explanation / condition tree"
      pattern: "blocked_reason|waiting_input|planned_actions"
    - from: "src/adk_agent/kairos/runtime.py"
      to: "src/adk_agent/kairos/api.py"
      via: "status route returns shared kairos semantics"
      pattern: "kairos|active_workflow|planned_actions|blocked_reason"
---

<objective>
建立 Phase 2 的共享 reporting 语义模型：让 runtime 产出 richer task summary、decision explanation 与 blocked condition tree，并通过兼容增强的 API 对外暴露。

Purpose: 先把“结果摘要/为什么继���/为什么停住”统一在 runtime 这一处生成，避免 runtime、API、UI 各自拼接不同语义。
Output: 可被前端直接消费的 `kairos` summary / explanation / condition-tree 字段，以及锁定这些字段的 runtime/API/Dex regression。
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-artifact-aware-reporting-visibility/02-CONTEXT.md
@.planning/phases/02-artifact-aware-reporting-visibility/02-RESEARCH.md
@.planning/phases/02-artifact-aware-reporting-visibility/02-VALIDATION.md
@.planning/phases/01-autonomous-continuation-mvp/01-03-SUMMARY.md
@src/adk_agent/kairos/models.py
@src/adk_agent/kairos/dex_bridge.py
@src/adk_agent/kairos/continuation.py
@src/adk_agent/kairos/runtime.py
@src/adk_agent/kairos/api.py

<interfaces>
From src/adk_agent/kairos/dex_bridge.py:
```python
@dataclass
class DexTaskSnapshot:
    task_id: str
    status: str
    description: str
    result: str = ""
    result_summary: str | None = None
    error_summary: str | None = None
    created_at: str | None = None
    completed_at: str | None = None
    log_path: str | None = None
```

From src/adk_agent/kairos/runtime.py:
```python
def get_status(self) -> dict:
    payload["tracked_dex_tasks"] = [{
        "task_id": task.task_id,
        "status": task.status,
        "description": task.description,
        "result": getattr(task, "result", ""),
        "result_summary": getattr(task, "result_summary", None),
        "error_summary": getattr(task, "error_summary", None),
        "log_path": getattr(task, "log_path", None),
    }]
    payload["active_workflow"] = asdict(self.state.active_workflow) if self.state.active_workflow else None
    payload["planned_actions"] = [asdict(action) for action in self.state.planned_actions]
    payload["blocked_reason"] = self.state.blocked_reason
```

From src/adk_agent/kairos/api.py:
```python
return {
    "status": "ok",
    "session_id": session_id,
    "kairos": status,
    "active_workflow": status.get("active_workflow"),
    "planned_actions": status.get("planned_actions", []),
    "blocked_reason": status.get("blocked_reason"),
}
```

From src/adk_agent/kairos/continuation.py:
```python
if state.policy.require_artifacts_before_follow_up and any(not self._path_exists(path) for path in required_artifacts):
    state.blocked_reason = "missing required artifacts for phase1 follow-up"
    workflow.status = "waiting_input"
    return []
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 生成 artifact-aware 结果摘要语义模型</name>
  <read_first>
    - src/adk_agent/kairos/models.py
    - src/adk_agent/kairos/dex_bridge.py
    - src/adk_agent/kairos/runtime.py
    - tests/kairos/test_runtime.py
  </read_first>
  <files>src/adk_agent/kairos/models.py, src/adk_agent/kairos/dex_bridge.py, src/adk_agent/kairos/runtime.py, tests/kairos/test_runtime.py</files>
  <behavior>
    - Test 1: 完成任务会生成结构化 summary 对象，至少包含状态、result_summary / error_summary、artifact 信息、log guidance。
    - Test 2: 失败任务默认返回“错误摘要 + 去哪里看日志/产物”的排查信息，而不是大段 result 原文。
    - Test 3: recent_events 继续保留时间线字符串，不承载新的结构化 summary 主体。
  </behavior>
  <action>按 D-01、D-02、D-03、D-08、D-10、D-12，在 runtime 侧新增单一 source-of-truth 的结果摘要字段。具体目标：1) 在 `models.py` 增加可序列化的 reporting 数据结构，承载当前结果摘要与每个 tracked Dex task 的 artifact-aware summary；2) 在 `dex_bridge.py` 从 Dex task 文件补齐 artifact 列表、log 定位提示、失败排查线索，供 runtime 直接消费；3) 在 `runtime.py:get_status()` 中输出新 summary 字段，并保持 `tracked_dex_tasks` / `recent_events` 的现有兼容字段仍可用；4) 在 `tests/kairos/test_runtime.py` 先写失败断言，再实现最小代码让其通过。不要把原始长日志塞进 API，不要把 summary 退化成只有 completed/failed 的平面文本。</action>
  <verify>
    <automated>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_runtime.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `KairosRuntime.get_status()` 返回新的结构化 summary 字段，且字段内容由 Dex snapshot 决定性拼装。
    - completed / failed 任务都能产出简洁摘要与日志/产物定位提示。
    - `recent_events` 仍然是时间线事件列表，没有被改造成 summary 容器。
  </acceptance_criteria>
  <done>运行时 status 已能稳定提供 artifact-aware 结果摘要，且 runtime 回归测试覆盖成功/失败两条路径。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 输出 why-continue / why-stop / condition-tree 解释模型并兼容接入 API</name>
  <read_first>
    - src/adk_agent/kairos/continuation.py
    - src/adk_agent/kairos/runtime.py
    - src/adk_agent/kairos/api.py
    - tests/kairos/test_runtime.py
    - tests/kairos/test_api.py
  </read_first>
  <files>src/adk_agent/kairos/models.py, src/adk_agent/kairos/continuation.py, src/adk_agent/kairos/runtime.py, src/adk_agent/kairos/api.py, tests/kairos/test_runtime.py, tests/kairos/test_api.py</files>
  <behavior>
    - Test 1: phase1 条件满足时，status/API 同时给出为什么继续与当前 planned action 的结构化解释。
    - Test 2: 缺失 artifact 或 waiting_input 时，status/API 给出完整条件树，包含 why_stop、missing_conditions 与当前 blocked_reason。
    - Test 3: `/kairos/status` 仍保留现有 `kairos` payload 与顶层 mirrors，不破坏旧消费路径。
  </behavior>
  <action>按 D-04、D-05、D-06、D-10、D-11、D-12，把 continuation 结果从一句 `blocked_reason` 扩展成共享 explanation/condition-tree 模型。具体目标：1) 在 `continuation.py` 评估当前 stage 时生成“为什么继续、为什么停住、还缺什么”的结构化对象，并把完整条件树编码进 state/status；2) 在 `runtime.py` 统一暴露 explanation 与 condition-tree 字段，且保留 rule-based continuation 边界，不引入新的自由规划能力；3) 在 `api.py` 兼容增强 `/api/sessions/{session_id}/kairos/status`，保留现有 mirrors，并只新增少量高价值 mirrors（如 current summary / decision explanation / missing conditions）用于前端渐进接入；4) 用 runtime/API 测试锁定 additive shape，避免顶层 mirrors 与 `kairos.*` 语义漂移。不要把 Phase 3 的 dedupe/max-step/history 扩进这次模型。</action>
  <verify>
    <automated>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_runtime.py tests/kairos/test_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `waiting_input` / blocked 场景能返回完整条件树与缺失条件列表。
    - status 与 API 都能说明 why_continue / why_stop，而不是仅剩 `blocked_reason` 平面字段。
    - 旧的 `kairos` payload、`active_workflow`、`planned_actions`、`blocked_reason` 顶层消费路径仍可工作。
  </acceptance_criteria>
  <done>KAIROS 的继续/停住决策已经可解释且可被 API 稳定消费，blocked 原因不再是不可展开的单句文本。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 用 Dex 集成回归锁定 artifact/log 摘要拼装行为</name>
  <read_first>
    - src/adk_agent/kairos/dex_bridge.py
    - tests/dex/test_tools.py
    - .planning/phases/02-artifact-aware-reporting-visibility/02-VALIDATION.md
  </read_first>
  <files>src/adk_agent/kairos/dex_bridge.py, tests/dex/test_tools.py</files>
  <behavior>
    - Test 1: 带 artifacts 的真实 Dex task 文件会被桥接成 richer summary 输入，而不是只剩 result 文本。
    - Test 2: 失败 Dex task 会给出 error_summary 与 log guidance，供 runtime/API 后续直接复用。
  </behavior>
  <action>在不引入新框架的前提下，扩充 Dex 集成回归样本，使 Phase 2 的 summary 语义有真实 task file 证据。具体目标：1) 在 `tests/dex/test_tools.py` 构造包含 artifact、log、result_summary/error_summary 的 Dex task fixture；2) 断言 `KairosDexBridge` 读出的快照足以支撑 Task 1/2 的 summary/explanation 模型；3) 若 bridge 需要补字段，���接在 `dex_bridge.py` 完成映射，不把这些推迟到前端 formatter。不要新增独立验证 harness，不要改写 Dex 主流程。</action>
  <verify>
    <automated>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/dex/test_tools.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Dex bridge 能稳定读取 artifact/log 相关信息，供 runtime summary 使用。
    - Dex 集成回归证明 summary 所需字段来自真实 task file，而不是 UI 侧拼接臆造。
  </acceptance_criteria>
  <done>Phase 2 的结果摘要模型已经有真实 Dex 文件级证据链，后续前端仅消费统一语义而不再反推原始任务文件。</done>
</task>

</tasks>

<verification>
- 先跑 `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_runtime.py -q` 锁定 runtime summary 行为。
- 再跑 `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_runtime.py tests/kairos/test_api.py -q` 锁定 explanation / API additive shape。
- 最后跑 `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/dex/test_tools.py -q` 确认 Dex snapshot 的 artifact/log ���据链路真实可用。
</verification>

<success_criteria>
- `kairos` payload 新增统一的 summary / explanation / condition-tree 字段，且不破坏旧字段。
- RPT-01 与 RPT-02 的核心语义先在 runtime/API 层落地，再由前端消费。
- Dex / runtime / API 三类测试共同证明结果摘要与缺失条件来自同一套模型，而非三处独立拼接。
</success_criteria>

<output>
After completion, create `.planning/phases/02-artifact-aware-reporting-visibility/02-01-SUMMARY.md`
</output>
