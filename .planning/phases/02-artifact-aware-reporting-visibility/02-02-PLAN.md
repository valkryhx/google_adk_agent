---
phase: 02-artifact-aware-reporting-visibility
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/adk_agent/static/index.html
  - src/adk_agent/static/script.js
  - tests/kairos/test_frontend_script_kairos_ui.py
  - tests/kairos/live_http_kairos_demo_outputs_regression.py
  - tests/kairos/test_live_http_kairos_demo_outputs_regression.py
autonomous: true
requirements: [RPT-03]
user_setup: []
must_haves:
  truths:
    - "用户能在 KAIROS 面板看到独立的结果摘要区，而不是只能看 tracked tasks 或 recent events。"
    - "用户能继续看到 Active Workflow、Planned Actions、Blocked Reason 三个区块，并在 blocked 场景下读到完整条件树。"
    - "recent_events 继续保持时间线角色，不与摘要区重复承载相同内容。"
  artifacts:
    - path: "src/adk_agent/static/index.html"
      provides: "保留三区并新增/补强结果摘要区的面板骨架"
      contains: "kairos"
    - path: "src/adk_agent/static/script.js"
      provides: "summary / blocked condition tree / timeline 分离渲染逻辑"
      contains: "refreshKairosStatus"
    - path: "tests/kairos/test_frontend_script_kairos_ui.py"
      provides: "前端 DOM 与 formatter 回归"
      contains: "kairos"
  key_links:
    - from: "src/adk_agent/static/script.js"
      to: "src/adk_agent/kairos/api.py"
      via: "refreshKairosStatus reads new kairos summary/explanation fields"
      pattern: "data.kairos|data\\."
    - from: "src/adk_agent/static/index.html"
      to: "src/adk_agent/static/script.js"
      via: "summary panel id wired into formatter output"
      pattern: "kairos.*Summary"
    - from: "tests/kairos/live_http_kairos_demo_outputs_regression.py"
      to: "src/adk_agent/static/script.js"
      via: "live status/UI visibility regression"
      pattern: "summary|blocked|recent_events"
---

<objective>
在保留现有 KAIROS 面板结构的前提下，新增或补强结果摘要区，并把 runtime/API 的共享 reporting 语义稳定映射到前端与 live regression。

Purpose: 把 Phase 2 的 richer summary 与 blocked explanation 变成真正可见、可调试、可 live 验证的 UI，而不是只停在 API 字段层。
Output: 新的 summary panel、增强后的 blocked reason 渲染、保持 timeline 角色的 recent events，以及锁定这些行为的前端/live 回归。
</objective>

<execution_context>
@C:/Users/Administrator/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-artifact-aware-reporting-visibility/02-CONTEXT.md
@.planning/phases/02-artifact-aware-reporting-visibility/02-RESEARCH.md
@.planning/phases/02-artifact-aware-reporting-visibility/02-VALIDATION.md
@.planning/phases/01-autonomous-continuation-mvp/01-03-SUMMARY.md
@.planning/phases/02-artifact-aware-reporting-visibility/02-01-PLAN.md
@src/adk_agent/static/index.html
@src/adk_agent/static/script.js
@tests/kairos/test_frontend_script_kairos_ui.py
@tests/kairos/live_http_kairos_demo_outputs_regression.py
@tests/kairos/test_live_http_kairos_demo_outputs_regression.py

<interfaces>
From src/adk_agent/static/index.html:
```html
<div id="kairosWorkflow">无</div>
<div id="kairosPlannedActions">无</div>
<div id="kairosBlockedReason">无</div>
<div id="kairosTrackedDexTasks">无</div>
<div id="kairosEvents">无</div>
```

From src/adk_agent/static/script.js:
```javascript
function formatKairosTrackedTasks(tasks) { ... }
function formatKairosWorkflow(workflow) { ... }
function formatKairosPlannedActions(actions) { ... }
function formatKairosEvents(events) { ... }

if (eventsEl) eventsEl.textContent = formatKairosEvents(kairos.recent_events || []);
if (trackedEl) trackedEl.textContent = formatKairosTrackedTasks(kairos.tracked_dex_tasks || []);
if (workflowEl) workflowEl.textContent = formatKairosWorkflow(kairos.active_workflow || data.active_workflow || null);
if (plannedActionsEl) plannedActionsEl.textContent = formatKairosPlannedActions(kairos.planned_actions || data.planned_actions || []);
if (blockedReasonEl) blockedReasonEl.textContent = kairos.blocked_reason || data.blocked_reason || '无';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在现有三区旁新增结果摘要区并定义 formatter 输出</name>
  <read_first>
    - src/adk_agent/static/index.html
    - src/adk_agent/static/script.js
    - tests/kairos/test_frontend_script_kairos_ui.py
    - .planning/phases/02-artifact-aware-reporting-visibility/02-01-PLAN.md
  </read_first>
  <files>src/adk_agent/static/index.html, src/adk_agent/static/script.js, tests/kairos/test_frontend_script_kairos_ui.py</files>
  <behavior>
    - Test 1: KAIROS modal 中新增独立结果摘要区，但 Active Workflow / Planned Actions / Blocked Reason 三块仍保留。
    - Test 2: summary formatter 消费 `kairos` 的共享 summary/explanation 字段，不解析 `recent_events` 字符串反推摘要。
    - Test 3: recent_events formatter 继续输出时间线文本，不与 summary panel 重复堆同一批内容。
  </behavior>
  <action>按 D-07、D-08、D-09、D-10、D-12，在 `index.html` 现有 KAIROS 区块中新增专门的结果摘要面板（建议放在 Blocked Reason 与 Tracked Dex Tasks 之间，避免打散已有三区），并在 `script.js` 增加对应 formatter。具体目标：1) 新面板用于展示“当前结果摘要 + 当前决策解释”，而不是复制 tracked tasks 原文；2) 保留 Active Workflow、Planned Actions、Blocked Reason 原 DOM id 与区块，不做单一 dashboard 重构；3) 为新面板增加可测试的 DOM id 与 formatter helper，并在前端测试中先锁定缺失面板/formatter 的失败断言。不要把 recent_events 改造成摘要区，不要移除 tracked tasks 面板。</action>
  <verify>
    <automated>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_frontend_script_kairos_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 面板结构中保留原有三区，并新增单独的结果摘要区。
    - `script.js` 存在专用 summary formatter/helper，且前端测试能定位到对应 DOM id。
    - `recent_events` 与 summary panel 的职责清晰分离。
  </acceptance_criteria>
  <done>前端骨架与 formatter 已支持独立 summary panel，且没有破坏既有自治状态展示区块。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把共享 reporting 语义接到摘要区与 blocked 条件树渲染</name>
  <read_first>
    - src/adk_agent/static/script.js
    - src/adk_agent/kairos/api.py
    - tests/kairos/test_frontend_script_kairos_ui.py
    - .planning/phases/02-artifact-aware-reporting-visibility/02-01-PLAN.md
  </read_first>
  <files>src/adk_agent/static/script.js, tests/kairos/test_frontend_script_kairos_ui.py</files>
  <behavior>
    - Test 1: refresh 路径优先消费 `data.kairos.*` 的 summary / explanation / condition-tree 字段，并只把 top-level mirrors 当兼容 fallback。
    - Test 2: blocked panel 可展示三段式解释与完整条件树，而不是只显示一句 `blocked_reason`。
    - Test 3: summary panel 展示当前结果总览，tracked tasks 继续展示 task-by-task 明细，二者不重复复制整段内容。
  </behavior>
  <action>按 D-04、D-05、D-07、D-08、D-10、D-11、D-12，更新 `refreshKairosStatus()` 与 formatter 组合。具体目标：1) 以 `kairos.summary` / `kairos.decision_explanation` / `kairos.condition_tree`（或 02-01 选定的等价字段）作为主数据源；2) blocked 区块要渲染“为什么继续/为什么停住/还缺什么”的完整可读文本，并展开缺失条件树；3) summary 区块只展示当前结果总览，不把 tracked task 明细全文再复制一遍；4) 仅在兼容必要时读取顶层 mirrors，避免 UI 与 API 语义漂移。不要新增复杂交互组件，不要把现有 formatter 架构重写成全新的前端框架。</action>
  <verify>
    <automated>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_frontend_script_kairos_ui.py -q</automated>
  </verify>
  <acceptance_criteria>
    - 前端 refresh 逻辑能渲染新的 summary / explanation / condition-tree 字段。
    - blocked 区块可读地表达 why_stop 与 missing conditions，且仍兼容旧 `blocked_reason` fallback。
    - tracked tasks / recent events / summary panel 各自职责清楚，没有三处重复同一段文本。
  </acceptance_criteria>
  <done>前端已经把 runtime/API 的共享 reporting 模型可视化出来，用户无需翻 `.dex/tasks/*.json` 即可理解当前自治状态与最近结果。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 用 live HTTP 回归证明摘要区与可解释状态对用户可见</name>
  <read_first>
    - tests/kairos/live_http_kairos_demo_outputs_regression.py
    - tests/kairos/test_live_http_kairos_demo_outputs_regression.py
    - tests/kairos/test_frontend_script_kairos_ui.py
    - .planning/phases/02-artifact-aware-reporting-visibility/02-VALIDATION.md
  </read_first>
  <files>tests/kairos/live_http_kairos_demo_outputs_regression.py, tests/kairos/test_live_http_kairos_demo_outputs_regression.py, tests/kairos/test_frontend_script_kairos_ui.py</files>
  <behavior>
    - Test 1: live status payload 在任务完成后包含 richer summary 字段，且 recent_events 仍是时间线。
    - Test 2: live 或可控回归样本能证明 blocked / waiting_input 路径对外暴露 inspectable missing-condition 数据。
    - Test 3: UI/API 最终状态可读，不需要人工打开 `.dex/tasks/*.json` 检查原始任务文件。
  </behavior>
  <action>把 Phase 2 的“可见性”要求写进现有 live regression 证据链。具体目标：1) 在 `tests/kairos/live_http_kairos_demo_outputs_regression.py` 与对应 pytest 包装测试中加入对 summary 字段、decision explanation、condition tree 或其兼容镜像的断言；2) 同步更新前端测试，确保新 summary panel 与 richer blocked rendering 在自动回归里有证据；3) 保留 live demo 当前自动 report 闭环，不引入新的服务依赖。不要把 live 测试改成只验证 raw JSON 存在，要验证“人类无需翻任务文件也能理解状态”的最终输出形态。</action>
  <verify>
    <automated>PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_frontend_script_kairos_ui.py tests/kairos/test_live_http_kairos_demo_outputs_regression.py -q</automated>
  </verify>
  <acceptance_criteria>
    - live/HTTP 回归证明 richer summary 与 blocked explanation 已经进入用户可见面。
    - 自动回归覆盖 summary panel、recent_events timeline 角色、以及 blocked condition tree 的外显行为。
  </acceptance_criteria>
  <done>Phase 2 的 UI 可见性已经有前端 + live HTTP 双重证据，RPT-03 不再只靠静态 DOM 描述成立。</done>
</task>

</tasks>

<verification>
- 先跑 `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_frontend_script_kairos_ui.py -q` 锁定 DOM 与 formatter。
- 再跑 `PYTHONPATH="D:/git_repos/google_adk_agent" PYTHONIOENCODING=utf-8 pytest tests/kairos/test_frontend_script_kairos_ui.py tests/kairos/test_live_http_kairos_demo_outputs_regression.py -q` 锁定可见性回归。
- 若需要补充端到端采样，按 02-VALIDATION.md 的 full suite 命令在 phase 结束前统一执行。
</verification>

<success_criteria>
- KAIROS 面板在保留现有三区的同时新增/补强结果摘要区。
- recent_events 继续是时间线，summary 区负责当前结果总览，blocked 区能展示完整条件树。
- 前端与 live 回归共同证明用户无需查看 `.dex/tasks/*.json` 即可理解自治 workflow 的当前结果与阻塞原因。
</success_criteria>

<output>
After completion, create `.planning/phases/02-artifact-aware-reporting-visibility/02-02-SUMMARY.md`
</output>
